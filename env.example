# LLM Provider API Keys (Required)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=...

# Site Configuration (Optional - auto-detected on Vercel)
# Set this to your production domain for proper OG image URLs
# If not set, will use VERCEL_URL or fallback to https://modeltriage.com
NEXT_PUBLIC_SITE_URL=https://modeltriage.com

# Supabase Auth (Required)
# Get these from your Supabase project dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Use either name — the app checks for both:
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_...  # (newer Supabase dashboards use this name)

# Supabase Service Role Key (Required for account deletion)
# Get this from Supabase dashboard → Settings → API → service_role key
# WARNING: Never expose this in the browser. Server-side only.
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Auth Dev Bypass (Optional — local development only)
# Set to "true" to skip auth and usage limits during local dev
# AUTH_DISABLED=true

# Usage Limits (Optional — defaults shown)
# ANONYMOUS_MAX_REQUESTS=3       # Lifetime cap for anonymous users
# FREE_DAILY_LIMIT=15            # Daily cap for free-tier users
# PRO_DAILY_LIMIT=200            # Daily cap for pro-tier users

# Vercel Auto-Provided (Do not set manually)
# VERCEL_URL - Automatically set by Vercel
